/*
La opción typedRoutes es una funcionalidad experimental introducida en versiones recientes de Next.js (13 o posterior).
Propósito: Habilitar soporte tipado para rutas en aplicaciones que usan TypeScript.
*/

const nextConfig = {
  experimental: {
    typedRoutes: true,
  },
};

export default nextConfig;
